# Adjust REPO_PATH if necessary to make sure it contains the path to the pulled Mudskipper repository when starting a skipper run
REPO_PATH = "path/to/Mudskipper"

########################################
# input files. 
MANIFEST: REPO_PATH + "/example/example_manifest"
barcode_csv: REPO_PATH + "/example/example_manifest"

# Path to location of Mudskipper run. 
WORKDIR: "path/to/workdir"

# Path to location of Mudskipper scripts.
SCRIPT_PATH: REPO_PATH + "/scripts"

########################################
# Background options

# If available, can provide external background which will replace CC. 
external_bam:
# If available, can provide an IgG antibody/spike-in/bead-only control for the multiplex experiment to serve as background
AS_INPUT:  # this must be one of the rows in barcode_csv

########################################
# Analysis options

# Specify which of the Dirichlet multinomial mixture (DMM) and beta-binomial mixture (BBM) models you want to run. 
DMM: True
BBM: True
# Setting both parameters to False will run the pre-processing with no further analysis. 

# Specify if you want to run finemapping analysis. Required for motif analysis with Homer.  
FINEMAPPING: False

# Specify which RBPs to undergo motif analysis via Homer (if left blank, then no motif analysis conducted).
# Requires finemapping. 
RBP_TO_RUN_MOTIF:
- RBFOX2

# Read type: Either "paired" for paired end or "single" for single end. 
READ_TYPE: "single"
# Single-end: enter 1. Paired-end: enter read (1 or 2) corresponding to crosslink site
INFORMATIVE_READ: 1

# Specify the seed (ensures reproducible model fitting, can generally be left as default)
SEED: 123

########################################
# Pre-processing options 

# adapter sequence to trim. Do not include barcode
adaptor_fwd: AGATCGGAAGAGCACACGTC
adaptor_red: # Leave blank if doing single end reads. 

# tile adapter sequences of this length
tile_length: 10

# cutadapt params.
QUALITY_CUTOFF: 6

# UMI_TOOLS
umi_pattern: XXXXXNNNNN

########################################
# Annotation parameters

# region caller

# Repeat table
REPEAT_TABLE: REPO_PATH + "/annotations/repeatmasker.grch38.tsv.gz" ###!!!### Do I need to change this to rmsk.txt.gz? 
# Genome fasta
GENOMEFA: REPO_PATH + "/annotations/GRCh38/GRCh38_no_alt_analysis_set_GCA_000001405.15.fasta"
# STAR reference
STAR_DIR: REPO_PATH + "/annotations/genome_ref" 
# Generated from STAR index
CHROM_SIZES: REPO_PATH + "/annotations/GRCh38/GRCh38_no_alt_analysis_set_GCA_000001405.15.chrom.sizes"

# Skipper will partition the transcriptome and create feature annotations from the GFF
PARTITION: REPO_PATH + "/annotations/gencode.v40.annotation.k562_totalrna.gt1.tiled_partition.bed.gz"
FEATURE_ANNOTATIONS: REPO_PATH + "/annotations/gencode.v40.annotation.k562_totalrna.gt1.tiled_partition.features.tsv.gz"

gencode.v38.annotation.k562_totalrna.gt1.gff3.gz
	
Added annotations and example run information
	
Aug 27, 2022
gencode.v38.annotation.k562_totalrna.gt1.tiled_partition.bed.gz


